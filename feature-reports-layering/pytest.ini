[pytest]
# Feature-root rootdir so the shared conftest.py (offline test env: SQLite +
# mock auth + mock Snowflake) applies across backend/tests and job/worker
# packages when scripts/validate.sh runs one pytest over all of them.
python_files = test_*.py
